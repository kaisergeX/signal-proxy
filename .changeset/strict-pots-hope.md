---
'@kaiverse/signal': minor
---

Add `withHMR` API and `hot` options for computeds & effects as a workaround to adapt with HMR mode in DEV. A dedicated bundler plugin that automates both is planned.
